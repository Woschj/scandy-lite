# Web-Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
jinja2==3.1.4
aiofiles==24.1.0

# Datenbank (PostgreSQL, async für die App, sync für Alembic-Migrationen)
sqlmodel==0.0.22
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Settings / Konfiguration
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Auth (lokal, Phase 1 - LDAP/SSO folgt später als eigenes Modul)
bcrypt==4.2.1
python-jose[cryptography]==3.3.0

# Formulare (HTMX/Alpine senden teils form-encoded)
python-multipart==0.0.20

# Dev/Test
pytest==8.3.4
pytest-asyncio==0.24.0
aiosqlite==0.20.0
httpx==0.28.1
