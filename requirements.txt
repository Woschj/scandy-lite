# Web-Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
jinja2==3.1.4
aiofiles==24.1.0
python-multipart==0.0.20
Pillow==10.4.0
# QR-Code-Erzeugung fuer Mitarbeiterausweise (app/core/badge.py) - nutzt
# Pillow (schon vorhanden) als Bild-Backend, keine weitere Abhaengigkeit.
qrcode==7.4.2

# Datenbank (PostgreSQL, async für die App, sync für Alembic-Migrationen)
sqlmodel==0.0.22
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Nur für den eingebauten Scandy2-Import (Admin -> Import): bson.json_util zum
# Parsen der von Scandy2 exportierten Backup-JSON (MongoDB Extended JSON -
# Daten/ObjectId-Typen). Kein laufender MongoDB-Server nötig.
pymongo==4.9.2

# Settings / Konfiguration
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Auth (lokal + optional SSO via OpenID Connect, siehe app/core/oidc.py)
bcrypt==4.2.1
python-jose[cryptography]==3.3.0
# Explizit deklariert statt nur transitiv über python-jose[cryptography] zu
# hängen - app/core/crypto.py importiert cryptography.fernet direkt (SMTP-
# Passwort-Verschlüsselung für EmailSettings), das darf nicht stillschweigend
# wegfallen, falls sich die jose-Extras mal ändern.
cryptography==43.0.3
# OIDC-Client fuer optionales SSO-Login (z.B. Authentik) - bewusst eine
# etablierte Bibliothek statt eigener Token-/JWKS-Verifikation: Fehler dabei
# (Signatur/aud/iss/exp nicht korrekt geprueft) sind genau die Art
# sicherheitskritischer Bugs, die man sich nicht selbst bauen sollte.
Authlib==1.3.2
# Von Authlibs Starlette-Integration fuer den kurzlebigen state/nonce
# waehrend des OIDC-Handshakes gebraucht (eigener, von der normalen
# Session getrennter signierter Cookie, siehe app/main.py).
itsdangerous==2.2.0

# Formulare (HTMX/Alpine senden teils form-encoded)

# Dev/Test
pytest==8.3.4
pytest-asyncio==0.24.0
aiosqlite==0.20.0
httpx==0.28.1
